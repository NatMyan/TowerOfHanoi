#ifndef REDEFINITIONS_HPP
#define REDEFINITIONS_HPP

#include <algorithm> //std::find
#include <string>
#include <vector>

using Disk = int;
using Disks = std::vector<Disk>;

using Menu = std::vector<std::string>;

using Key = int;

//using Printable = std::vector<std::string>;

using Option = std::string;

#endif //REDEFINITIONS_HPP